# API Cost Optimization Flow Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Client Request                              │
│                     (article text + endpoint)                       │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Authentication & Rate Limiting                   │
│  • Check session token                                              │
│  • Verify rate limits (5 req/min)                                   │
│  • Return 401/429 if failed                                         │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│               🚀 OPTIMIZATION 1: Dynamic Token Allocation           │
│  calculateOptimalTokens(articleLength, endpoint)                    │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ Article Length  →  Token Allocation                           │ │
│  │ ───────────────    ─────────────────                          │ │
│  │ 500 chars       →  1,500 tokens (37% of max 4000)            │ │
│  │ 1,500 chars     →  2,750 tokens (69% of max)                 │ │
│  │ 3,000+ chars    →  4,000 tokens (100% of max)                │ │
│  └───────────────────────────────────────────────────────────────┘ │
│  Savings: 15-25% on output tokens                                   │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│               🚀 OPTIMIZATION 2: Response-Level Caching             │
│  isResponseCachingEnabled() → true?                                 │
└────────────┬────────────────────────────────────────────────────────┘
             │
     ┌───────┴────────┐
     │                │
     ▼                ▼
┌─────────────┐  ┌──────────────────────────────────────────────────┐
│ Cache       │  │ Cache                                            │
│ DISABLED    │  │ ENABLED                                          │
└──────┬──────┘  └──────┬───────────────────────────────────────────┘
       │                │
       │                ▼
       │         ┌────────────────────────────────────────────────┐
       │         │ 1. Hash article content (SHA-256)              │
       │         │    → "a1b2c3d4e5f6g7h8"                        │
       │         └──────┬─────────────────────────────────────────┘
       │                │
       │                ▼
       │         ┌────────────────────────────────────────────────┐
       │         │ 2. Check cache:                                │
       │         │    Key: api-response:{endpoint}:{hash}         │
       │         └──────┬─────────────────────────────────────────┘
       │                │
       │        ┌───────┴────────┐
       │        │                │
       │        ▼                ▼
       │   ┌─────────┐     ┌─────────┐
       │   │Cache HIT│     │Cache    │
       │   │(95% save)│    │MISS     │
       │   └────┬────┘     └────┬────┘
       │        │               │
       │        │               └────────┐
       │        │                        │
       └────────┼────────────────────────┘
                │
                ▼
         ┌──────────────┐
         │Return cached │
         │response with │
         │metadata      │
         └──────┬───────┘
                │
                └──────────────────────────────────────┐
                                                       │
                ┌──────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Call Claude API (on cache miss)                  │
│  anthropic.messages.create({                                        │
│    model: "claude-sonnet-4-20250514",                               │
│    max_tokens: optimalTokens,  // ← Dynamic allocation              │
│    system: [{                                                       │
│      type: "text",                                                  │
│      text: "...",                                                   │
│      cache_control: { type: "ephemeral" }  // ← Prompt caching     │
│    }],                                                              │
│    messages: [{ role: "user", content: articleText }]               │
│  })                                                                 │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Process Claude API Response                      │
│  • Extract token usage (input, output, cache read/write)           │
│  • Calculate costs using Anthropic pricing                          │
│  • Parse JSON response                                              │
│  • Validate response structure                                      │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│               🚀 OPTIMIZATION 3: Cost Monitoring                    │
│  logAPIRequest({                                                    │
│    endpoint, inputTokens, outputTokens,                             │
│    cacheReadTokens, cacheHit, articleLength,                        │
│    responseTime                                                     │
│  })                                                                 │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ Logs stored in memory (last 10,000 requests)                  │ │
│  │ • Per-request cost calculation                                │ │
│  │ • Aggregation into daily summaries                            │ │
│  │ • Cache hit rate tracking                                     │ │
│  │ • Budget alert monitoring                                     │ │
│  └───────────────────────────────────────────────────────────────┘ │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Cache Response (if enabled)                      │
│  cacheAnalysis(articleText, endpoint, response, TTL)                │
│  • Store in MemoryCacheService                                      │
│  • 24-hour expiration (default)                                     │
│  • LRU eviction when full                                           │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Add Cache Metadata                               │
│  {                                                                  │
│    ...response,                                                     │
│    _cache: {                                                        │
│      cachedAt: "2025-01-27T10:00:00Z",                              │
│      requestHash: "a1b2c3d4e5f6g7h8",                               │
│      cacheStatus: "hit" | "miss",                                   │
│      timeSaved: 3200,  // milliseconds                              │
│      costSavings: "$0.016-0.020"                                    │
│    }                                                                │
│  }                                                                  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Return to Client                                 │
│  NextResponse.json(enhancedResponse)                                │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Cost Monitoring Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Every API Request                              │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
                  ┌──────────────────────────┐
                  │   logAPIRequest()        │
                  │   • Calculate costs      │
                  │   • Store in memory      │
                  │   • Log to console       │
                  └──────────┬───────────────┘
                             │
             ┌───────────────┼───────────────┐
             │               │               │
             ▼               ▼               ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │ Daily        │ │ Real-time    │ │ Budget       │
    │ Aggregation  │ │ Logging      │ │ Monitoring   │
    └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
           │                │                │
           ▼                ▼                ▼
    ┌──────────────────────────────────────────────┐
    │      Cost Dashboard API                      │
    │  /api/cost-dashboard?action=                 │
    │  • summary     - Today's totals              │
    │  • report      - Full text report            │
    │  • range       - Date range analysis         │
    │  • logs        - Recent requests             │
    │  • cache-stats - Cache performance           │
    │  • export      - JSON export                 │
    │  • optimize-   - AI recommendations          │
    │    recommendations                            │
    └──────────────────────────────────────────────┘
```

---

## Token Allocation Curve

```
max_tokens
    │
4000├─────────────────────────────────────────────── maxTokens
    │                                          ┌────┐
    │                                     ┌────┘    │
3500│                                ┌────┘         │
    │                           ┌────┘              │
3000│                      ┌────┘                   │
    │                 ┌────┘                        │
2750├────────────────┼──────────────────────────── 50% + (50% scaled)
    │           ┌────┘                              │
2500│      ┌────┘                                   │
    │ ┌────┘                                        │
2000│─┘                                             │
    │                                               │
1500├───────────────────────────────────────────── minTokens
    │
    └────┬──────────┬──────────┬──────────┬────────┬─► article length (chars)
         500      1000       1500       2000     3000
         │          │          │          │
      50% tokens  midpoint  75% tokens  90%
```

---

## Cache Hit Flow

```
First Request (Cache MISS)
─────────────────────────
Time: 3200ms
Cost: $0.020
Steps:
  1. Calculate hash      (1ms)
  2. Check cache (miss)  (1ms)
  3. Call Claude API     (3100ms) ← EXPENSIVE
  4. Store in cache      (5ms)
  5. Return response     (93ms)

Second Request (Cache HIT)
──────────────────────────
Time: 150ms  (95% faster!)
Cost: $0.001 (95% cheaper!)
Steps:
  1. Calculate hash      (1ms)
  2. Check cache (hit)   (1ms)  ← Fast lookup
  3. Return cached       (148ms)
     (skip steps 3-4)
```

---

## Cost Breakdown Example

```
Endpoint: /api/analyze-article-full
Article Length: 2500 characters

WITHOUT OPTIMIZATIONS:
━━━━━━━━━━━━━━━━━━━━━
Fixed max_tokens: 4000
Input tokens:     2000 × $3/M    = $0.006000
Output tokens:    4000 × $15/M   = $0.060000
Cache read:       1800 × $0.3/M  = $0.000540
                                   ─────────
Total:                             $0.066540

WITH OPTIMIZATIONS (First Request):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Dynamic max_tokens: 2750 (31% savings on output)
Input tokens:     2000 × $3/M    = $0.006000
Output tokens:    2750 × $15/M   = $0.041250 ← 31% less
Cache read:       1800 × $0.3/M  = $0.000540
                                   ─────────
Total:                             $0.047790
Savings:                           $0.018750 (28% reduction)

WITH OPTIMIZATIONS (Cached Request):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Cache hit - no API call!
Lookup cost:      ~0              = $0.000100 (negligible)
                                   ─────────
Total:                             $0.000100
Savings:                           $0.066440 (99.8% reduction!)

AVERAGE (40% cache hit rate):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
60% misses:  $0.047790 × 0.6  = $0.028674
40% hits:    $0.000100 × 0.4  = $0.000040
                                ─────────
Average cost:                   $0.028714
Original cost:                  $0.066540
TOTAL SAVINGS:                  $0.037826 (57% reduction)
```

---

## Budget Alert System

```
┌─────────────────────────────────────────────────────┐
│         Daily Budget Monitoring                     │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
              ┌────────────┐
              │Every request│
              │logged with  │
              │cost         │
              └──────┬─────┘
                     │
                     ▼
              ┌────────────────────────┐
              │Calculate daily total   │
              │Sum all request costs   │
              └──────┬─────────────────┘
                     │
                     ▼
              ┌────────────────────────┐
              │Compare to budget limit │
              │(from env var)          │
              └──────┬─────────────────┘
                     │
         ┌───────────┴───────────┐
         ▼                       ▼
   ┌──────────┐           ┌──────────────┐
   │Under     │           │Over budget!  │
   │budget    │           │Trigger alert │
   │✓ OK      │           │⚠ WARNING     │
   └──────────┘           └──────┬───────┘
                                 │
                                 ▼
                          ┌──────────────┐
                          │• Log warning │
                          │• Email admin │
                          │• Dashboard   │
                          │  shows alert │
                          └──────────────┘
```

---

## Integration Points

### Where Optimizations Apply

```
┌──────────────────────────────────────────────────────────────┐
│ API Route: /api/analyze-article-full/route.optimized-v2.ts  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│ Line 142: Dynamic Token Allocation                          │
│   const optimalTokens = calculateOptimalTokens(...)         │
│   logTokenAllocation(...)                                   │
│                                                              │
│ Line 158: Response Caching (Check)                          │
│   const cached = await getCachedAnalysis(...)               │
│   if (cached) return cached;                                │
│                                                              │
│ Line 230: Claude API Call                                   │
│   max_tokens: optimalTokens  // ← Dynamic!                  │
│                                                              │
│ Line 290: Cost Monitoring                                   │
│   logAPIRequest({ endpoint, tokens, cost, ... })            │
│                                                              │
│ Line 350: Response Caching (Store)                          │
│   await cacheAnalysis(articleText, endpoint, result)        │
│                                                              │
│ Line 360: Metadata Enhancement                              │
│   addCacheMetadata(response, cacheHit, hash)                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## File Dependencies

```
Optimized API Routes
├── app/utils/dynamicTokens.ts
│   ├── calculateOptimalTokens()
│   ├── logTokenAllocation()
│   └── estimateTokenSavings()
│
├── app/lib/responseCache.ts
│   ├── getCachedAnalysis()
│   ├── cacheAnalysis()
│   ├── addCacheMetadata()
│   ├── hashArticleContent()
│   └── isResponseCachingEnabled()
│   │
│   └── app/services/cache/MemoryCacheService.ts
│       ├── get()
│       ├── set()
│       ├── has()
│       ├── delete()
│       └── getStats()
│
└── app/utils/costMonitor.ts
    ├── logAPIRequest()
    ├── getDailySummary()
    ├── generateCostReport()
    ├── checkBudgetAlert()
    └── exportLogsToJSON()

Cost Dashboard API
├── app/api/cost-dashboard/route.ts
    ├── GET ?action=summary
    ├── GET ?action=report
    ├── GET ?action=range
    ├── GET ?action=logs
    ├── GET ?action=cache-stats
    ├── GET ?action=export
    └── GET ?action=optimize-recommendations
```

---

This diagram illustrates the complete flow of all three optimizations working together to achieve 54-62% cost reduction while maintaining response quality and adding comprehensive monitoring capabilities.
