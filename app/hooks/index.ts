/**
 * Custom Hooks Index
 *
 * @description Central export point for all custom hooks
 * Enables clean imports: import { useAnalysis, useClipboard } from @/app/hooks
 */

export { useAnalysis } from "./useAnalysis";
export { useClipboard } from "./useClipboard";
