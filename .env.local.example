# =============================================================================
# note-hashtag-ai-generator Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to Git!

# -----------------------------------------------------------------------------
# Anthropic API Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Authentication Configuration (REQUIRED FOR SECURITY)
# -----------------------------------------------------------------------------

# JWT Secret for token signing (MINIMUM 32 characters)
# Generate a strong secret with:
#   openssl rand -base64 32
#   OR
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-strong-256-bit-secret-here-minimum-32-characters-required

# Membership Password (MINIMUM 8 characters)
# This is the password users must enter to access the application
# Generate a strong password with:
#   openssl rand -base64 24
MEMBERSHIP_PASSWORD=your-strong-membership-password-here

# -----------------------------------------------------------------------------
# Rate Limiting Configuration (OPTIONAL)
# -----------------------------------------------------------------------------

# Maximum API requests allowed per time window
# Default: 5 requests per minute
# Increase for development, decrease for stricter protection
API_RATE_LIMIT_MAX_REQUESTS=5

# Time window for rate limiting in milliseconds
# Default: 60000 (1 minute)
# 60000 = 1 minute, 300000 = 5 minutes
API_RATE_LIMIT_WINDOW_MS=60000

# -----------------------------------------------------------------------------
# Development Environment (OPTIONAL)
# -----------------------------------------------------------------------------

# Node environment
# Development: development (default)
# Production: production
NODE_ENV=development

# -----------------------------------------------------------------------------
# Security Notes
# -----------------------------------------------------------------------------
# 1. NEVER use default/example values in production
# 2. Rotate secrets every 3-6 months
# 3. Keep this file secure and never share it
# 4. Add .env.local to .gitignore (already done)
# 5. Use strong, randomly generated secrets
# 6. Different secrets for development and production
# 7. Back up your secrets securely (password manager recommended)
