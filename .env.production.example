# =============================================================================
# Success Recipe Finder - Production Environment Variables
# =============================================================================
#
# IMPORTANT: Copy this file to .env.production and fill in your actual values
#
# For deployment, set these variables in:
# - Vercel: Dashboard → Settings → Environment Variables
# - Railway: Dashboard → Variables
# - Render: Dashboard → Environment
#
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (REQUIRED)
# -----------------------------------------------------------------------------
# Production database connection string
#
# Vercel Postgres example:
# DATABASE_URL="postgres://default:xxx@xxx.vercel-storage.com:5432/verceldb?sslmode=require"
#
# Supabase example:
# DATABASE_URL="postgresql://postgres:pass@db.xxx.supabase.co:5432/postgres"
#
# Neon example:
# DATABASE_URL="postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"
#
DATABASE_URL="postgresql://username:password@host:5432/database?sslmode=require"

# -----------------------------------------------------------------------------
# AUTHENTICATION (REQUIRED)
# -----------------------------------------------------------------------------
# JWT secret for session tokens
# MUST be 32+ characters for security
#
# Generate with: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
#
JWT_SECRET="REPLACE_WITH_RANDOM_32_PLUS_CHARS_FROM_OPENSSL_RAND_BASE64"

# Admin password for /admin panel access
# MUST be 12+ characters with uppercase, lowercase, numbers, and symbols
# Example: Secure$RecipeF1nd3r!2025
#
ADMIN_PASSWORD="Your$ecureP@ssw0rd2025!MinimumTwelveChars"

# -----------------------------------------------------------------------------
# NODE ENVIRONMENT (AUTO-SET by most platforms)
# -----------------------------------------------------------------------------
NODE_ENV="production"

# -----------------------------------------------------------------------------
# OPTIONAL: API Features (if using Claude AI features)
# -----------------------------------------------------------------------------
# Anthropic API Key for AI-powered features
# Get your API key from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY="sk-ant-api03-xxx"

# -----------------------------------------------------------------------------
# OPTIONAL: Performance & Caching
# -----------------------------------------------------------------------------
# Enable production caching for better performance
# ENABLE_CACHING="true"
# CACHE_TTL_MS="1800000"

# Enable usage analytics
# ENABLE_USAGE_ANALYTICS="true"

# =============================================================================
# DEPLOYMENT CHECKLIST
# =============================================================================
#
# Before deploying:
# ✓ Generate strong JWT_SECRET with: openssl rand -base64 32
# ✓ Set ADMIN_PASSWORD to 12+ characters with mixed case, numbers, symbols
# ✓ Set DATABASE_URL to your production database
# ✓ Never commit .env.production to git
# ✓ Set environment variables in hosting platform (Vercel/Railway/Render)
# ✓ Run: npx prisma migrate deploy (after setting DATABASE_URL)
#
# After deploying:
# ✓ Visit /admin and login with ADMIN_PASSWORD
# ✓ Upload CSV file to import articles
# ✓ Verify security headers with: curl -I https://your-domain.com
# ✓ Test article search at /articles
#
# =============================================================================
